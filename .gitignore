# Rust
/wasm-core/target
/wasm-core/pkg
# Node / Next
/web/node_modules
/web/.next
/web/out
# WASM build output (regenerate with: wasm-pack build wasm-core --target web --out-dir ../web/src/wasm)
/web/src/wasm
# Playwright MCP artifacts (E2E verification)
/.playwright-mcp
/*.png
# OS
Thumbs.db
.DS_Store
# Prospecção: saída do web/scripts/extract-leads.mjs — dados pessoais de
# terceiros (telefone/WhatsApp/CNPJ). O repo é público, não vai pra main.
/leads-bruto.csv
/leads-dedup.csv
/_leads.jsonl
